{
  "explorer.fileNesting.enabled": true,
  "explorer.fileNesting.patterns": {
    "*.res": "${capture}.mjs, ${capture}.res.js, ${capture}.gen.tsx, ${capture}.cmi, ${capture}.cmt, ${capture}.cmj",
    "*.resi": "${capture}.res"
  },
}